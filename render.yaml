services:
  - type: web
    name: insta360-backend
    env: python
    rootDir: insta360_flutter/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.6
